'use strict';const test=require('node:test');const assert=require('node:assert/strict');const {evaluate}=require('../domain');const ctx={actor:'taxpayer-1'};const base={ownerId:'taxpayer-1',taxYear:'2024',jurisdiction:'US-FEDERAL',form:'1040',filingStatus:'single',rulePackVersion:'irs-2024-final-v1',authoritativeCitations:[{sourceRef:'irs:1040-2024',retrievedAt:'2026-07-18'}],documents:[{documentRef:'w2:1',sha256:'a'.repeat(64),reconciliationStatus:'matched'}],calculationLines:[{lineId:'1z',amount:100,sourceDocumentRef:'w2:1'},{lineId:'adjustment',amount:-20,sourceDocumentRef:'doc:2'}]};test('tax calculation is deterministic, source-mapped and review gated',()=>{const out=evaluate(base,ctx);assert.deepEqual(out,evaluate(base,ctx));assert.equal(out.result.calculatedTotal,80);assert.equal(out.result.credentialedPreparerRequired,true);assert.equal(out.result.taxAdvice,false);});test('unsupported combinations and uncertain positions fail closed',()=>{const out=evaluate({...base,taxYear:'2026',uncertainPosition:true,positionMemoRef:'',documents:[]},ctx);assert.ok(out.errors.length>=3);});
